[pytest]
markers =
    unit: Unit tests (business logic, isolated)
    api: API tests (REST contracts, status codes)
    integration: Integration tests (cross-service communication)
    workflow: End-to-end business scenarios
